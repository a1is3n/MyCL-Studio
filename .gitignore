# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Coverage / test output
coverage
e2e/artifacts/

# Secrets & local runtime state (NEVER commit) — keys live in ~/.mycl, not here
.env
.env.*
secrets.json
auth.json
*.key
*.pem
*.p12
.mycl/

# Build bundles / installers
*.dmg
*.app

# Local-only docs & archives — no code impact, kept out of git (see nogit/README)
/nogit/

# Local-only dev process (auto-loaded by Claude Code; mutates per development)
/dev.md
# Not: CLAUDE.md artık git'te (YZLLM 2026-06-20: "git'e de al, gizli bilgi yok") — katı kural kaynağı.
